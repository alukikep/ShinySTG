# ShinySTG 项目 Cline 规则

> 进入项目必读的精简提示。其他细节按需查阅:
> - 项目概览 → `README.md`(特性表 + 跳转,**不复制架构细节**)
> - 架构地图 / 板块索引 / 编写规范 → `ARCHITECTURE.md`(根,**薄**,只 100+ 行)
> - 单子体系架构细节 → `docs/architecture/arch-<name>.md`(**架构的单一真相源**,每个子系统一块)
> - 开发约定 / 编码规范 / 工具链踩坑 → `CONTRIBUTING.md`
> - 关卡编辑器使用 → `LEVEL_EDITOR.md`
> - 音频子系统(Inspector / SO 创建步骤)→ `Assets/Scripts/Audio/README.md`

实现功能后,**仅更新对应 `docs/architecture/arch-<name>.md`**(单一真相源);`ARCHITECTURE.md` 仅在"加新板块"时改 §1/§3 一行摘要;`README.md` 特性表保持薄描述。

---

# Unity Coding Rules

## Code Editing

* 修改 `.cs` 源代码时，优先使用 `editor` 或 `apply_patch`。
* 不要使用 `bash`、`python`、`sed`、`awk`、`perl`、`cat >`、heredoc 等方式直接生成或修改 `.cs` 文件。
* Terminal 主要用于执行命令、运行测试、Git 操作和构建验证，而不是作为代码编辑器。

## Unity Validation

完成代码修改后，不要自行创建或执行额外的静态代码检查脚本来模拟 Unity 编译。

不要通过以下方式宣称代码已经编译通过：

文件数量检查
namespace 检查
大括号检查
正则表达式检查
API 字符串检查
文件存在性检查
自己编写的“compile check”脚本

C# 代码的最终编译验证由 Unity Editor 完成。

完成代码修改后：

确保代码文件已经正确保存。
不需要自行验证 Unity 编译结果。
在最终总结中明确说明：
"代码修改已完成，Unity 编译尚未验证，请在 Unity Editor 中触发编译并检查 Console。"
不要声称“编译通过”“0 errors”或“PR 已完全验证”，除非实际获得了 Unity Editor 的编译结果。

如果用户之后提供 Unity Console 的错误信息，再根据真实的 compiler error 定位和修复代码。
## Completion Criteria

除非真实验证已经完成，不要使用：

* “已完成”
* “落地完成”
* “验证通过”
* “0 errors”
* “PR 完成”

如果只完成了代码文件，应明确写：

> “代码层实现完成，Unity Editor 集成和运行验证尚未完成。”

## Verification Report

最终报告必须区分：

* File-level validation
* Compile validation
* Runtime validation
* Unity Editor integration

不得将文件层面的静态检查描述为编译或运行验证。
